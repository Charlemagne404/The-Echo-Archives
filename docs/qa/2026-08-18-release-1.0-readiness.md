# Release 1.0 Documentation And Readiness — 2026-08-18

## Scope

This report is the current release-status record for the 1.0 documentation
baseline. It updates the active docs to describe the shipped static-first
product and the current post-release follow-up. It does not replace the
historical July launch audit or claim that a local run proves production host,
provider, recovery, monitoring, or physical-device readiness.

## Current release baseline

The generated catalog snapshot dated 2026-08-16 records:

| Metric | Value |
| --- | ---: |
| Published shows | 724 |
| Imported shows | 523 |
| Indexed-only shows | 194 |
| Full reviews | 7 |
| Collections | 38 |
| Creator-verified shows | 0 |
| Shows missing RSS | 3, all documented |
| Research-gap records | 14 |

The current product surface includes browse/search, structured filters,
collections, show pages, moderated contributions, community rating
infrastructure, protected submission/import/elevation/collection-maintainer
workflows, generated SEO output, and an offline fallback. Ask the Archivist is
preserved but disabled by default in 1.0.

## Release gate status

| Area | Status | Evidence or remaining action |
| --- | --- | --- |
| Product documentation | Current | Active source-of-truth docs now describe the 1.0 baseline and post-release work. |
| Catalog report | `content-pending` | Two records lack verified runtime duration and an explicit research-gap note: `big-grande-teachers-lounge` and `machina`. |
| Catalog validation | Pass | 724 shows and 38 collections validate successfully. |
| Backend tests | Pass | 271 tests passed. |
| Configuration check | Pass | Development configuration preflight passed. |
| Link check | Pass | Local links, anchors, and assets validated across 17 public pages, 724 shows, and 38 collections. |
| Repository structure check | Blocked | Existing `shared/styles/home/cards/17-responsive-780-a.css` is 551 lines, one line over the hard limit. |
| Tool/operations tests | Environment-blocked | 41 passed, 9 failed, and 3 skipped; failures include macOS-incompatible GNU `stat -c`/`date --` assumptions and Restic recovery-tool checks. |
| Full root verification | Not established | The root gate was not called green; it includes the catalog report and structure checks above. |
| Production host and proxy | Unverified | Requires target-host Caddy, TLS, DNS, HSTS, origin, redirect, and `/api/health` checks. |
| Backups and recovery | Unverified | Requires configured off-host retention and an independent restore drill. |
| External providers and monitoring | Unverified | Requires live Turnstile/rating state, access logs, uptime/error/backup monitoring, and provider checks. |
| Physical browser coverage | Unverified | Requires current iOS Safari and Android Chrome checks in addition to automated smoke coverage. |

## Open catalog objectives

The two runtime blockers must be resolved with evidence or explicit
`metadata.researchGaps` entries before the generated catalog report can be
described as complete. The archive also has 0 creator-verified live records and
only 7 full reviews; both are visible post-release editorial objectives, not
facts to imply away in product copy.

## Commands run

- `npm run report:catalog` — exited non-zero with the two catalog blockers; generated output drift was clean.
- `npm --prefix backend run validate:data` — passed for 724 shows and 38 collections.
- `npm --prefix backend test` — passed 271 tests.
- `npm run check:config` — passed for development configuration.
- `npm --prefix backend run check:links` — passed for 17 public pages, 724 shows, and 38 collections.
- `npm run check:structure` — failed on the existing 551-line CSS hard-limit violation; soft-limit warnings were also reported.
- `npm run test:tools` — 41 passed, 9 failed, and 3 skipped because this macOS environment lacks the GNU `stat`/`date` behavior expected by existing operations tests and does not have Restic installed.

Focused local evidence is not a substitute for the target-host and physical
device gates. Update this report or add a newer dated QA report when those
gates are actually verified.
