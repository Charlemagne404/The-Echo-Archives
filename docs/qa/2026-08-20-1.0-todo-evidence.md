# 1.0 TODO Closeout And Evidence Handoff — 2026-08-20

## Status

The two catalog TODOs are complete. The five legal, privacy, provider, and
production TODOs remain evidence-gated: this repository pass supplies the
implementation, checklist, and local evidence, but it cannot prove the state
of the production host, Cloudflare account, deployed HTML, or counsel's legal
decisions.

No secrets, backup credentials, private submission content, or personal data
are recorded in this report.

## Catalog evidence

### Big Grande Teachers' Lounge

The protected maintainer import/elevation path audited the current official
sources and published the source-backed update:

- [Big Grande official website](https://biggrandewebsite.com/) links the current
  [Apple Podcasts listing](https://podcasts.apple.com/us/podcast/the-teachers-lounge-seasons-1-9/id1777965588)
  and [Art19 RSS feed](https://rss.art19.com/60dd5309-db63-4b1a-9051-27e4c72958e5).
- The observed feed contains 161 full entries with duration coverage of 1.0:
  51-minute average, 63-minute median, 1-minute minimum, 89-minute maximum,
  and 136.8 observed hours. The catalog uses the existing `at-least` count
  qualifier and does not claim that the feed is a complete historical archive.
- The record remains an Imported factual record. Human-owned fields were
  preserved, and the source/runtime evidence is retained in the import
  provenance under `catalog-src/shows/big-grande-teachers-lounge.json`.

### Machina

The current [Machina RSS feed](https://feeds.megaphone.fm/machina),
[Apple listing](https://podcasts.apple.com/us/podcast/machina/id1560995601?uo=4),
and linked [Realm show page](https://www.realm.fm/shows/machina) were checked.
The feed exposes one 61-second trailer and no full episodes; no full-episode
runtime can be verified from the current RSS/Apple evidence, and the Realm page
currently returns 404. No duration was invented. The authoritative record now
contains the approved `metadata.researchGaps` note and corresponding external
research provenance.

The protected importer correctly left the Machina candidate at maintainer
review because its source scope signal required review. The published factual
record was preserved and only the evidence-backed research-gap fallback was
added.

### Previous RSS audit

The audit for `impact-winter`, `homecoming`, `earth-eclipsed`,
`the-rapscallion-agency`, and `the-invenios-expeditions` remains unchanged.
The three missing RSS links (`earth-eclipsed`, `the-rapscallion-agency`, and
`the-invenios-expeditions`) remain documented, as do the three pre-existing
runtime unknowns (`the-rapscallion-agency`, `the-invenios-expeditions`, and
`edict-zero-fis`). Ambiguous or shared RSS identities were not restored.
Machina's new explicit gap is the fourth documented runtime-unknown record in
the current generated report.

## Local catalog and repository evidence

The generated report now records 724 published shows, 523 Imported records,
194 indexed-only records, 7 full reviews, and 38 collections. It reports zero
blocking errors, zero actionable RSS gaps, zero editorial gaps, zero taxonomy
unknown/non-approved tags, and clean generated-output drift.

Commands completed successfully:

- `npm run build:catalog`
- `npm run build:pages`
- `npm run report:catalog`
- `npm --prefix backend run validate:data`
- `npm --prefix backend run check:links`
- `node --test --test-concurrency=1 backend/test/*.test.js` — 276 passed, 0 failed
- `npm run check:config`
- `NODE_ENV=production npm run check:config` — valid production configuration;
  it warned that maintainer authentication is disabled in this local
  environment, so that warning is not production-host evidence
- `npm run check:structure` — completed with existing soft-limit warnings; no
  hard-limit failure
- `git diff --check`
- `npm run verify` — completed successfully; its nested backend verification
  included 276 backend tests and 63 browser smoke tests

The legal-acknowledgement fixture in the rate-limit test was updated to the
already-declared `2026-08-20` version. The full backend suite now verifies that
current acknowledgements are accepted and stale/missing acknowledgements are
rejected by the server contract.

`npm run test:tools` now passes 51 of 54 tests, with 3 Restic integration tests
skipped because `restic` is not installed on this macOS host. The earlier
macOS `stat`/`date`, guarded recovery-path, missing-backup-directory, and
temporary-cleanup failures are covered by portable implementations and
cross-platform fixtures. This is repository evidence only, not production
backup evidence.

Browser evidence is complete for the checked-in Playwright smoke coverage: the
aggregate `backend` smoke wrapper passed 63 tests with 0 failures, including
the read-only, chat/submit, and community-rating flows. Production iOS Safari,
Android Chrome, and deployed-host checks remain open.

## Production evidence checklist

Complete this section on the production host without pasting secrets or
private submission data into the repository:

- [ ] Record the deployed commit/application version and confirm it matches the
  reviewed source.
- [ ] Show successful SQLite cleanup journal entries for the deployed service;
  confirm the configured values for `COMMUNITY_ABUSE_RETENTION_DAYS` (30),
  `COMMUNITY_PROFILE_METADATA_RETENTION_DAYS` (30),
  `COMMUNITY_ORPHAN_PROFILE_RETENTION_DAYS` (90),
  `SUBMISSION_NETWORK_DATA_RETENTION_DAYS` (30),
  `SUBMISSION_PERSONAL_DATA_RETENTION_DAYS` (180), and
  `DATA_RETENTION_CLEANUP_INTERVAL_MS` (300000), recording values but never
  secrets.
- [ ] Confirm `echo-archives.service`, `echo-archives-backup.timer`, and
  `echo-archives-offsite-backup.timer` are enabled/active as intended, and
  inspect the corresponding service results and journal logs.
- [ ] Confirm the newest local completed backup is fresh, the count is at least
  7, and backup files/directories have the intended private ownership and
  permissions.
- [ ] Confirm the off-site Restic policy is 7 daily, 5 weekly, 12 monthly, and
  2 yearly snapshots, grouped by the Echo host/tag.
- [ ] Confirm local completed-backup pruning uses 30 days with a minimum of 7
  copies and runs only after off-site verification.
- [ ] Record a successful off-site snapshot ID, restore-drill date/duration,
  and freshness marker without recording credentials.
- [ ] On the restored copy, require `PRAGMA integrity_check` = `ok`, an empty
  `PRAGMA foreign_key_check`, expected non-empty table checks, and the checked-in
  application/representative-page restore verification.

The checked-in sources for this evidence are
[`deploy/OFF_HOST_BACKUP_PLAN.md`](../../deploy/OFF_HOST_BACKUP_PLAN.md),
[`deploy/echo-archives-offsite-backup.sh`](../../deploy/echo-archives-offsite-backup.sh),
and [`docs/OPERATIONS.md`](../OPERATIONS.md). If the host intentionally differs
from this workflow, record the deviation and update the authoritative Privacy
source before closure.

## Cloudflare, ratings, and analytics checklist

- [ ] Record the production Cloudflare Challenge Passage configuration value
  (without credentials or unrelated account data).
- [ ] Confirm whether Turnstile is enabled for rating writes and that the
  configured site-key mapping is the intended production mapping. Record only
  enabled/disabled state and non-secret identifiers; never record the secret
  key.
- [ ] Confirm Cloudflare RUM is enabled as described by the deployed policy,
  or record the actual provider behavior for a policy update.
- [ ] Record the effective production values of
  `COMMUNITY_RATING_WRITES_ENABLED` and `COMMUNITY_TURNSTILE_ENABLED`, plus
  whether the site-key/secret-key pair is complete, without exposing key
  values.
- [x] Local check: `PLAUSIBLE_DOMAIN` is unset in `backend/.env` and generated
  public HTML contains no Plausible script tag.
- [ ] Production check: confirm `PLAUSIBLE_DOMAIN` is unset in the deployed
  environment and deployed public HTML contains no Plausible script.
- [ ] If another analytics processor is active, update the authoritative
  Privacy and Cookies sources and add the required consent behavior before
  deployment. Do not close this item from local configuration alone.

The current policy sources intentionally avoid claiming a fixed Challenge
Passage lifetime or that Turnstile is active. They should be changed only after
the provider/host evidence is known.

## Counsel brief and decisions required

The current authoritative policy sources identify Charlie Arnerstål as the
controller, describe The Echo Archives as a Continental project rather than a
separate legal entity, and state that the controller is established in Sweden.
The contact routes are `privacy@echoarchives.net` and the Continental contact
route. The current map covers:

- browsing/request and Cloudflare security data, including possible
  `cf_clearance` handling;
- anonymous/pseudonymous community rating identifiers and optional Turnstile
  tokens;
- submission content, optional contact email, source IP, user agent, and
  moderation records;
- Cloudflare edge/security/Turnstile and RUM processing, external listening
  links, Patreon/Ko-fi/GitHub links, and the Continental contact route;
- the implemented retention rules: 30-day network/abuse windows, 30-day
  profile metadata inactivity, 90-day orphan profile cleanup, 180-day
  unpublished submission content/contact retention, 14-day optional access
  telemetry, published content while published, and backup expiry under the
  checked-in recovery policy;
- a general-audience, not-child-directed service and a rights process covering
  access, correction, deletion, restriction, objection, and withdrawal where
  applicable, with the Swedish supervisory-authority route described;
- a voluntary copyright/rights-request workflow that currently makes no
  designation of a DMCA agent and no statutory notice/counter-notice or
  safe-harbor promise.

Obtain written decisions on:

- [ ] whether a published postal address is required and, if so, the exact
  approved address;
- [ ] whether a DPO is legally required or voluntarily appointed and the exact
  approved details;
- [ ] whether an EU/EEA or other representative is required and the exact
  approved details;
- [ ] whether jurisdiction-specific consumer, privacy, children’s, or other
  notices must be added;
- [ ] whether a designated copyright agent or statutory notice process is
  required for any applicable jurisdiction, and the exact procedure/agent
  details;
- [ ] whether any current legal-basis, provider, retention, or children’s
  wording needs revision.

Only after those decisions should the authoritative sources
[`site-src/pages/privacy.html`](../../site-src/pages/privacy.html),
[`site-src/pages/cookies.html`](../../site-src/pages/cookies.html),
[`site-src/pages/terms.html`](../../site-src/pages/terms.html), and
[`site-src/pages/copyright.html`](../../site-src/pages/copyright.html) be
changed, followed by `npm run build:pages`, policy/source parity checks, public
route checks, and the stale-legal-version test.

The current legal document version is `2026-08-20`. If counsel changes the
legal date, update these existing constants together and test them together:

- `shared/app/submit/api.js`
- `backend/lib/services/submission-service.js`
- `backend/test/data-retention.test.js`
